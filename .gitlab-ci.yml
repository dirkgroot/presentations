stages:
  - publish

build:
  stage: publish
  pages: true
  image: node:20-alpine
  before_script:
    - yarn install
  script:
    - yarn build
  artifacts:
    paths:
      - dist
  cache:
    key:
      files:
        - yarn.lock
    paths:
      - node_modules
